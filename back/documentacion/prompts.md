# Prompts

---

## Prompt 1

se nos pidió sincronizar con el modelo de datos PERO no podemos crear las tablas en sql nosotros mismos, nos exigieron que todo lo tiene que manejar hibernate. lo que no sabemos es si deberiamos crear una base de datos sin ninguna tabla (asumiendo que hibernate la va a complear solo), o conectar una base de datos con tablas vacias. o si ni siquiera debemos crearla base de datos y la crea sola con spring.datasource.url=jdbc:mysql://localhost:3306/ComidaApp. como podemos hacer para saber si están sincronizandose los datos
